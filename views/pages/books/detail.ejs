<section class ="book-view container">
    <ul id ="book-list">
        <%for(var i of books) { %>
        <li class ="book-item" book-id="<%= task.id %>">
            <h2>Title:<%= book.title%></h2>
            <p>Author:<% book.Author %></p>
            <p>Description:<%book.description %></p>
            <p>ISBN: <% book.isbn %></p>
            <p>bookshelf:<% bookshelf.book %></p>
            <p>image:<% image.book-image_url %></p>
           
    </li> <% } %>
        </ul>
</section>